{
  "name": "uselessprojecttkhub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run build && cd .. && rm -rf backend/dist && cp -r frontend/dist backend/",
    "start": "cd backend && npm start"
  }
}
