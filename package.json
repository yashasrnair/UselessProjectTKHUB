{
  "name": "uselessprojecttkhub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install && npm install react-router-dom && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start"
  }
}
